{
  "name": "changeset-demo-monorepo",
  "private": true,
  "description": "Standalone demo replicating the xmcp Changesets release setup.",
  "license": "MIT",
  "scripts": {
    "build": "turbo run build",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "publish-packages": "changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.30.0",
    "turbo": "^2.6.3"
  },
  "packageManager": "pnpm@10.8.1",
  "engines": {
    "node": "20.x"
  }
}
